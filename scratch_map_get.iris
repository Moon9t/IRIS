def main() -> i64 {
    val m = map()
    map_set(m, "a", 42);
    val v = map_get(m, "a");
    if is_some(v) {
        print("Found!");
        print(to_str(unwrap(v)));
    } else {
        print("Not found!");
    }
    0
}
