def ok(b: bool) -> i64 { if b { 0 } else { 1 } }

record Point { x: i64, y: i64 }
choice Color { Red, Green, Blue }
choice Shape { Circle(i64), Square(i64) }

trait Double { def double(x: i64) -> i64 }
impl Double for i64 { def double(x: i64) -> i64 { x * 2 } }

def add(a: i64, b: i64) -> i64 { a + b }
def greet(name: str = "iris") -> str { concat("hello ", name) }
def id[T](x: T) -> T { x }
def first[T, U](a: T, b: U) -> T { a }
def make_point(x: i64) -> Point { Point { x: x, y: x + 1 } }

def test_add_1() -> i64 { ok(1 + 2 == 3) }
def test_add_2() -> i64 { ok(10 + 5 == 15) }
def test_sub_1() -> i64 { ok(9 - 4 == 5) }
def test_sub_2() -> i64 { ok(3 - 8 == -5) }
def test_mul_1() -> i64 { ok(3 * 4 == 12) }
def test_mul_2() -> i64 { ok(7 * 0 == 0) }
def test_div_1() -> i64 { ok(9 / 3 == 3) }
def test_div_2() -> i64 { ok(20 / 4 == 5) }
def test_mod_1() -> i64 { ok(10 % 3 == 1) }
def test_mod_2() -> i64 { ok(14 % 5 == 4) }
def test_precedence_1() -> i64 { ok(1 + 2 * 3 == 7) }
def test_precedence_2() -> i64 { ok((1 + 2) * 3 == 9) }
def test_negation_1() -> i64 { ok(0 - 5 == -5) }
def test_negation_2() -> i64 { ok(-7 == -7) }
def test_cmp_lt() -> i64 { ok(2 < 3) }
def test_cmp_le() -> i64 { ok(3 <= 3) }
def test_cmp_gt() -> i64 { ok(5 > 2) }
def test_cmp_ge() -> i64 { ok(5 >= 5) }
def test_cmp_eq() -> i64 { ok(4 == 4) }
def test_cmp_ne() -> i64 { ok(4 != 5) }

def test_if_else() -> i64 {
    val x = if 1 < 2 { 3 } else { 4 }
    ok(x == 3)
}

def test_if_else_nested() -> i64 {
    val x = if 1 > 2 { 0 } else { if 2 > 1 { 5 } else { 6 } }
    ok(x == 5)
}

def test_while_sum() -> i64 {
    var i = 0
    var sum = 0
    while i < 5 {
        sum = sum + i
        i = i + 1
    }
    ok(sum == 10)
}

def test_while_break() -> i64 {
    var i = 0
    while i < 10 {
        if i == 3 { break }
        i = i + 1
    }
    ok(i == 3)
}

def test_while_continue() -> i64 {
    var i = 0
    var sum = 0
    while i < 5 {
        i = i + 1
        if i == 3 { continue }
        sum = sum + i
    }
    ok(sum == 12)
}

def test_for_sum() -> i64 {
    var sum = 0
    for i in 0..5 {
        sum = sum + i
    }
    ok(sum == 10)
}

def test_for_zero() -> i64 {
    var sum = 1
    for i in 0..0 {
        sum = 0
    }
    ok(sum == 1)
}

def test_loop_break() -> i64 {
    var x = 0
    loop {
        x = 7
        break
    }
    ok(x == 7)
}

def test_short_circuit_and() -> i64 { ok((true && false) == false) }
def test_short_circuit_or() -> i64 { ok((false || true) == true) }

def test_tuple_literal() -> i64 {
    val t = (1, 2)
    ok(t.0 == 1)
}

def test_tuple_index() -> i64 {
    val t = (1, 2, 3)
    ok(t.2 == 3)
}

def test_tuple_destructure() -> i64 {
    val (a, b) = (3, 4)
    ok(a + b == 7)
}

def test_tuple_nested() -> i64 {
    val t = ((1, 2), 3)
    val a = t.0
    ok(a.1 == 2)
}

def test_array_literal() -> i64 {
    val a = [1, 2, 3]
    ok(a[0] == 1)
}

def test_array_index() -> i64 {
    val a = [5, 6, 7]
    ok(a[2] == 7)
}

def test_array_store() -> i64 {
    var a = [1, 2, 3]
    a[1] = 9
    ok(a[1] == 9)
}

def test_array_sum_loop() -> i64 {
    val a = [1, 2, 3, 4]
    var i = 0
    var sum = 0
    while i < 4 {
        sum = sum + a[i]
        i = i + 1
    }
    ok(sum == 10)
}

def test_array_sum_for() -> i64 {
    val a = [1, 2, 3]
    var sum = 0
    for i in 0..3 {
        sum = sum + a[i]
    }
    ok(sum == 6)
}

def test_array_reassign() -> i64 {
    var a = [1, 2, 3]
    a = [4, 5, 6]
    ok(a[0] == 4)
}

def test_record_literal() -> i64 {
    val p = Point { x: 1, y: 2 }
    ok(p.x + p.y == 3)
}

def test_record_field() -> i64 {
    val p = Point { x: 5, y: 7 }
    ok(p.x == 5)
}

def test_record_return() -> i64 {
    val p = make_point(4)
    ok(p.y == 5)
}

def test_choice_variant() -> i64 {
    val c = Color.Red
    val v = when c {
        Color.Red => 1,
        _ => 0,
    }
    ok(v == 1)
}

def test_choice_when() -> i64 {
    val c = Color.Green
    val v = when c {
        Color.Red => 1,
        Color.Green => 2,
        Color.Blue => 3,
    }
    ok(v == 2)
}

def test_choice_with_data() -> i64 {
    val s = Shape.Circle(3)
    val v = when s {
        Shape.Circle(r) => r,
        Shape.Square(x) => x,
    }
    ok(v == 3)
}

def test_choice_payload_sum() -> i64 {
    val s = Shape.Square(4)
    val v = when s {
        Shape.Circle(r) => r,
        Shape.Square(x) => x + 1,
    }
    ok(v == 5)
}

def test_option_when() -> i64 {
    val o = some(4)
    val v = when o {
        some(x) => x,
        none => 0,
    }
    ok(v == 4)
}

def test_result_when() -> i64 {
    val r = ok(7)
    val v = when r {
        ok(x) => x,
        err(e) => 0,
    }
    ok(v == 7)
}

def test_option_unwrap() -> i64 {
    val o = some(9)
    ok(unwrap(o) == 9)
}

def test_function_call() -> i64 { ok(add(2, 3) == 5) }
def test_default_param() -> i64 { ok(greet() == "hello iris") }
def test_generic_identity() -> i64 { ok(id(3) == 3) }
def test_generic_first() -> i64 { ok(first(9, "x") == 9) }
def test_trait_double() -> i64 { ok(double(21) == 42) }

def test_closure_basic() -> i64 {
    val f = |x: i64| x + 1
    ok(f(4) == 5)
}

def test_closure_capture() -> i64 {
    val n = 5
    val f = |x: i64| x + n
    ok(f(7) == 12)
}

def test_closure_two_params() -> i64 {
    val f = |a: i64, b: i64| a * b
    ok(f(3, 4) == 12)
}

def test_string_len() -> i64 {
    val s = "hello"
    ok(len(s) == 5)
}

def test_string_concat() -> i64 {
    val s = concat("a", "bc")
    ok(len(s) == 3)
}
