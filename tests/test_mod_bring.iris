mod math {
    pub def add(a: i64, b: i64) -> i64 {
        a + b
    }
    pub def mul(a: i64, b: i64) -> i64 {
        a * b
    }
}

bring math

def main() -> i64 {
    val sum = math__add(2, 3);
    println("math.add = " + to_str(sum));

    val prod = math__mul(4, 5);
    println("math.mul = " + to_str(prod));

    if sum == 5 {
        if prod == 20 {
            println("All module bring tests passed!");
            0
        } else {
            println("mul wrong");
            1
        }
    } else {
        println("add wrong");
        1
    }
}
