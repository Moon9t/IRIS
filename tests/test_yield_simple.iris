def count_to(n: i64) -> list<i64> {
    var i = 1;
    while i <= n {
        yield i;
        i = i + 1;
    }
}

def main() -> i64 {
    val counts = count_to(3);
    val c_len = len(counts);
    if c_len != 3 { return 1 };
    0
}
