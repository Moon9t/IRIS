choice Color { Red, Green, Blue }

def test_exhaustive_with_wildcard() -> i64 {
    val c = Color.Red
    when c {
        Color.Red => 1,
        _ => 0,
    }
}

def main() -> i64 {
    test_exhaustive_with_wildcard()
}