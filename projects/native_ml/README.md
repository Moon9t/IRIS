# Native Machine Learning

A demonstration of IRIS's native Machine Learning capabilities. 
This project builds, trains, and evaluates a Multi-Layer Perceptron (MLP) entirely from scratch using `std.nn` without relying on external frameworks or weights.

## Building

```bash
iris build projects/native_ml/main.iris -o native_ml
```

## Usage

```bash
./native_ml
```
