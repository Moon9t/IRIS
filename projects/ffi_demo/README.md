# FFI Demo

A demonstration of IRIS's Foreign Function Interface (FFI) capabilities.
This project uses the Python C-API bindings in `std.ffi` to execute Python code natively from IRIS.

## Building

```bash
iris build projects/ffi_demo/main.iris -o ffi_demo
```

## Usage

```bash
./ffi_demo
```
